#!/usr/bin/python3
def best_score(a_dictionary):
    if not a_dictionary:
        return None

    for key, value in a_dictionary.items():
        if value > alexa_value:
            alexa_value = value
            ihotu_key = key

    return (ihotu_key)
