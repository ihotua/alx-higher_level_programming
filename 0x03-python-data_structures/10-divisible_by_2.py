#!/usr/bin/python3
def divisible_by_2(my_list=[]):
    ihotu = []
    for i in range(len(ihotu)):
        if ihotu[i] % 2 == 0:
            ihotu.append(True)
        else:
            ihotu.append(False)
    return (ihotu)
