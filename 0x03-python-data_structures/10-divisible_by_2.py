#!/usr/bin/python3
def divisible_by_2(my_list=[]):
    ihotu = []
    for num in range(len(ihotu)):
        if ihotu[num] % 2 == 0:
            ihotu[num] = True
        else:
            ihotu[num] = False
    return (ihotu)
